// Grace à une boucle et un test inclus dans la boucle, affichez uniquement les nombres pairs

for (i = 1; 1 <= 10; i++) {
    if (i % 2 == 0) { // modulo pour determiner si le nombre est pair
        console.log(i);
    }
}